# 提交规则

## 提交格式

统一使用：

```text
编号:类型:说明
```

示例：

```text
005:更新:优化 one-click 部署脚本服务选择
006:修复:修正 HTTP 文件服务启动提示
007:文档:更新 README 当前目录说明
```

## 类型

- `新增`：新增脚本、功能或配置
- `修复`：修复错误、兼容性问题或异常行为
- `更新`：调整已有功能、交互、默认值或输出说明
- `重构`：不改变功能前提下整理代码结构
- `文档`：更新 README、说明文档或注释
- `清理`：删除无用文件、旧目录或过期内容
- `回滚`：撤销某次改动

## 约定

- 每次提交尽量只做一类事情。
- 说明使用中文，尽量短，直接写清楚改了什么。
- 涉及部署行为变化时，同步更新 `README.md`。
- 不提交运行时生成目录，例如 `generated-stack/`、缓存目录和临时文件。

## 版本号规则（语义化版本）

镜像 tag 与 `Cargo.toml` 的 `version` 保持一致，格式 `vMAJOR.MINOR.PATCH`：

| 提交类型 | 版本变化 | 示例 |
| --- | --- | --- |
| `新增`（新功能） | MINOR +1，PATCH 归零 | 1.2.0 → 1.3.0 |
| `修复`（bug） | PATCH +1 | 1.2.0 → 1.2.1 |
| `更新` / `重构` / `文档` / `清理` | 不单独发版，随下一次「新增」或「修复」一起升 | — |
| 破坏性变更（改配置格式、认证模型、目录结构等） | MAJOR +1 | 1.x → 2.0.0 |

发布步骤（三步，workflow 见到 `v*` tag 会自动构建并推镜像）：

```bash
# 1. 改 Cargo.toml 的 version
# 2. 提交
git add -A && git commit -m "029:新增:xxx"
# 3. 打 tag 并推送 → 自动触发 GitHub Actions 构建
git tag v1.2.0 && git push origin main --tags
```

### 版本里程碑

| 版本 | 内容 |
| --- | --- |
| v1.0.0 | 中文界面改造 + README / Docker Hub 门面美化（001-005） |
| **v1.1.0** | **账号体系与权限设置**：网页首次初始化管理员、登录后改密码、凭据文件保护、会话 Cookie 登录、访客访问范围（all/public/none）、独立设置页、公开目录、匿名直链开关、原生登录框修复（006-027） |
| v1.2.0 | 待发布（下一次「新增」功能时使用） |

> ⚠️ **版本号只在实际发布时才递增**。若某一版规划的内容还没发布就被下一批功能覆盖，
> 应**合并成同一个版本号**，不要跳号。
> 例：2026-09-20 当天先规划了「v1.1.0 账号体系」「v1.2.0 权限设置」两个里程碑，
> 但中间态一次都没正式发布，最终合并为 **v1.1.0** 一次发布 —— 跳过 v1.1.0 直接叫 v1.2.0
> 会让人误以为中间少了一版。

### 注意

**不要为每次迭代单独推一个版本号。** 中间态（改了还没定型、纯粹为了测试）直接用
`latest` 覆盖或本地 `docker build` 验证即可。Docker Hub 上只保留里程碑版本，
避免出现「一天推 22 个版本（v1.0.1 … v1.0.22）」这种无法阅读的历史。
